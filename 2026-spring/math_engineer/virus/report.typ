#import "/template.typ": setup, frame
#show: setup

#let my-title = "会話距離とマスクによる感染確率の数値シミュレーション"
#let my-author = "今島隼人"
#let my-date = datetime(year: 2026, month: 6, day: 7)

#set document(
  title: my-title,
  author: my-author,
  date: my-date,
)

#align(center)[
  #text(size: 19pt, weight: "bold")[会話距離とマスクによる感染確率の\ 数値シミュレーション]
  #v(0.6em)
  #my-author
  #v(0.3em)
  #my-date.display("[year]-[month]-[day]")
]

= 目的

2人が向き合って60分間会話する簡易モデルについて、話者間距離、マスク、感染力の違いが感染確率に与える影響を調べる。Aが感染者、Bが非感染者であるとし、1分ごとにAまたはBが等確率で発話する。Aが発話した分だけBが飛沫を浴び、暴露量の総和が15以上になったとき感染したと判定した。

= シミュレーション方法

距離を $d$ m、感染力倍率を $v$、マスクを通過する割合を $m$ とすると、Aが1分発話したときのBの暴露量を次のようにした。

$
  e = v m / d^3
$

マスクなしでは $m=1$、ウレタンマスクでは40%カットなので $m=0.60$、不織布マスクでは75%カットなので $m=0.25$ とした。課題1と2では各距離で10000回試行し、課題3では感染力を0.5倍刻みで変えながら、100回の試行で一度も感染しない最小距離を0.1 m刻みで探索した。乱数は固定seedを使い、再実行しても同じ結果になるようにした。

= 結果

== マスクの効果

#figure(
  image("mask_effect.png", width: 92%),
  caption: [マスク条件ごとの距離と感染確率],
)

マスクなしでは1.2 mで感染確率が0.8709、1.3 mで0.2556、1.4 mで0.0014となり、この付近で急に下がった。ウレタンマスクでは同じ低下が1.0 mから1.2 m付近に移り、不織布マスクでは0.8 mから0.9 m付近まで近づいた。簡易モデル上では、マスクにより暴露量が直接減るため、同じ距離でも感染確率が大きく下がることが分かる。

== 感染力の効果

#figure(
  image("virulence_effect.png", width: 92%),
  caption: [マスクなしで感染力を1倍から5倍にしたときの感染確率],
)

感染力が大きいほど、感染確率が下がり始める距離は右に移動した。感染力1倍では1.4 mでほぼ0になったが、2倍では1.7 m、3倍では2.0 mでほぼ0になった。一方、4倍と5倍では2.0 mでも感染確率が残り、特に5倍では2.0 mでも0.9556であった。感染力が高い条件では、通常の対人距離だけでは十分でなく、換気、会話時間の短縮、マスクの併用が必要になると考えられる。

== 安全な距離

#figure(
  image("safe_distance.png", width: 92%),
  caption: [感染力ごとの100回で一度も感染しない最小距離],
)

#table(
  columns: (1fr, 1fr),
  inset: 6pt,
  stroke: 0.5pt,
  table.header([感染力倍率], [安全な距離]),
  [1.0倍], [1.4 m],
  [1.5倍], [1.6 m],
  [2.0倍], [1.8 m],
  [2.5倍], [1.9 m],
  [3.0倍], [2.1 m],
  [3.5倍], [2.2 m],
  [4.0倍], [2.3 m],
  [4.5倍], [2.3 m],
  [5.0倍], [2.4 m],
)

感染力が大きくなると安全な距離も増加した。ただし、この「安全」は100回のシミュレーションで一度も感染しなかった距離という意味であり、絶対に感染しない距離ではない。確率の見積もりとしては試行回数を増やすほど安定するが、今回の課題3の条件に合わせて100回で判定した。

#pagebreak()

= 学内向け注意喚起ポスター

#block(
  fill: rgb("#f7fbff"),
  stroke: 1.2pt + rgb("#1f6fb2"),
  inset: 18pt,
  width: 100%,
  radius: 4pt,
)[
  #align(center)[
    #text(size: 22pt, weight: "bold", fill: rgb("#174c82"))[会話は距離・マスク・時間でリスクを下げよう]
  ]

  #v(1em)

  #grid(
    columns: (1fr, 1fr),
    gutter: 14pt,
    [
      #text(size: 15pt, weight: "bold")[シミュレーションから分かったこと]
      - マスクなし60分会話では、1.2 mでも高い感染確率になった。
      - 不織布マスクは暴露量を大きく下げ、ウレタンマスクより効果が大きい。
      - 感染力が高い条件では、2 mでも十分でない場合がある。
    ],
    [
      #text(size: 15pt, weight: "bold")[キャンパスで意識したい行動]
      - 長く話すときは不織布マスクを使う。
      - 近距離での会話は短くする。
      - 教室や研究室では換気し、人が密集する場所を避ける。
    ],
  )

  #v(1em)
  #image("mask_effect.png", width: 100%)

  #v(0.8em)
  #align(center)[
    #text(size: 13pt, weight: "bold", fill: rgb("#b00020"))[
      距離だけに頼らず、マスク・換気・短時間化を組み合わせることが重要です。
    ]
  ]
]

= 考察

このモデルでは、暴露量が距離の3乗に反比例するため、感染確率はある距離を境に急激に変化した。マスクは暴露量に掛ける係数として表したので、不織布マスクの75%カットはグラフ上でも大きな差として現れた。また感染力が高い場合には、安全な距離が伸びるだけでなく、2 m以内の範囲では感染確率が下がりきらない条件もあった。

ただし、この計算は発話者、距離、飛沫量、感染判定を単純化したモデルであり、現実の感染リスクをそのまま表すものではない。実際には換気、声量、部屋の広さ、会話の向き、体調、マスクの着け方なども影響する。そのため、結果は「距離を取る」「不織布マスクを使う」「長時間の会話を避ける」という注意喚起の根拠として読むのが適切である。

= 添付コード

グラフ1から3は `main.rs` のRustプログラムで作成した。実行すると `mask_effect.png`、`virulence_effect.png`、`safe_distance.png`、`results.csv` が生成される。
